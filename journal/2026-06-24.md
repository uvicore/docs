# 2026-06-24

## Documented leaner OpenAPI / Swagger config (framework 0.4.3)

- **Intent:** Document the two new `api.openapi` config knobs that tame large OpenAPI schema sets
  (a VFI app's `openapi.json` was ~235 KB and the Swagger UI was sluggish).
- **Changed:**
  - `docs/http/api/openapi.md` — added `separate_schemas` + `docs.models_expansion` to the config
    block and a new "Taming Large Schema Sets" section explaining the Input/Output collapse and
    hiding the Swagger Schemas section.
  - `docs/epologue/changelog/0.4.md` — new **0.4.3** entry.
  - `docs/epologue/release-notes.md` — 0.4.3 bullet.
- **Related:** framework journal 2026-06-24 (code + tests).

## Per-operation Swagger knobs (follow-up)

- **Intent:** Document the per-operation expand fix (expanding one endpoint still lagged after the
  Schemas section was hidden).
- **Changed:** `docs/http/api/openapi.md` — added `model_expansion` and the `parameters` passthrough
  to "Taming Large Schema Sets", plus a note that these render the schema lazily rather than shrink
  it. Updated the `0.4.3` changelog entry and the release-notes bullet accordingly.
