# 2026-06-23

## Expanded RAW SQL execution-helper reference
- **Intent:** The execution-helpers table listed only 4 helpers; `db.py` has 13.
- **Changed:** Rebuilt the table grouped by return shape (many rows / one row / scalars / writes),
  with per-method behavior (which throw vs return `None`). Noted helpers accept a SQLAlchemy query
  object and a `metakey=`.
- **Files:** `docs/docs/database/db-sa-raw.md`.

## Removed obsolete `update_forward_refs()` from ORM docs
- **Intent:** Pydantic v2 made per-model `update_forward_refs()` obsolete (central rebuild at boot).
- **Changed:** Dropped the call from the model examples; rewrote the warning to explain forward-ref
  types are imported at the bottom of the file and rebuilt centrally — no call needed.
- **Files:** `docs/docs/database/orm-model.md`, `orm-basics.md`.
- **Related:** framework [ADR 0003](../../framework/adr/0003-central-model-rebuild.md);
  schematic `uvicore-database` skill updated.

## Documented relation key derivation + composite keys
- **Intent:** The `local_key`/`foreign_key` auto-derivation was never spelled out; composite keys
  only had a brief tip here vs. a full section in `orm-querybuilder.md`.
- **Changed:** Added a "How local_key and foreign_key are derived" subsection (per-relation rules +
  explicit-equivalent examples) to `orm-model.md`; expanded the composite-keys tip (positional,
  same-length, not derived, shard key first) while keeping the full treatment in `orm-querybuilder.md`.
- **Files:** `docs/docs/database/orm-model.md`.
- **Related:** framework [ADR 0006](../../framework/adr/0006-composite-multi-column-relation-keys.md).

## Converted `vfi` examples to the `acme.wiki` paradigm
- **Intent:** All docs examples must use the `acme.wiki` demo namespace; composite-key examples used
  a `vfi` repair-order domain.
- **Changed:** Mapped `vfi.core`→`acme.wiki`, `ros`/`ro_items`→`posts`/`comments`, shard keys
  `qgroup_id`/`client_id`→`tenant_id`/`workspace_id`, natural keys `key`/`ro_key`→`id`/`post_id`,
  consistently across all four pages.
- **Files:** `db-sa-queries.md`, `db-queries.md`, `orm-querybuilder.md`, `orm-model.md` (under
  `docs/docs/database/`).

## Added "where knowledge lives" convention + established journal/ADR
- **Changed:** Added the repo-vs-machine-local knowledge note to `CLAUDE.md`; created this `journal/`
  and `adr/` at repo root with READMEs/templates.
- **Files:** `CLAUDE.md`, `journal/*`, `adr/README.md`.

## Strengthened docs authoring guidance for consistency
- **Intent:** Audit whether `.claude` + `CLAUDE.md` are enough for best/consistent docs. Found two
  real gaps causing the drift we kept fixing.
- **Changed:** (1) Added a canonical **`acme.wiki` demo-domain** section to the `uvicore-docs` skill
  (entity/table/column/relation table + "never invent a namespace" rule) and a pointer in
  `CLAUDE.md` — the root cause of `vfi`/`Forecast`/`invoices` drift. (2) Fixed the epologue changelog
  filename convention in both the skill and `CLAUDE.md` (`<major.minor>.md` e.g. `0.4.md`, not the
  documented-but-wrong `0.4.0.md`).
- **Files:** `.claude/skills/uvicore-docs/SKILL.md`, `CLAUDE.md`.
- **Related:** also fixed a stale "docs branch 0.3" claim in `framework/CLAUDE.md` (it's `0.4`).
