# 2026-07-01

## Document Redis connection `options`

- **Intent:** Document the new (framework 0.4.5) `options` dict on a Redis connection, which passes
  arbitrary keyword arguments straight through to the redis-py async client (`from_url()`) — the
  Redis analogue of the database connection's `options` → SQLAlchemy `connect_args`.
- **Changed:** `docs/deeper/redis.md` — added `options` to the Configuration example block and a
  paragraph explaining it (accepts any `from_url()` kwarg: `health_check_interval`,
  `socket_timeout`, `max_connections`, `decode_responses`…; omit for client defaults), linking to
  the database `db-config.md` for the parallel. `epologue/changelog/0.4.md` — new `## 0.4.5`
  section. `epologue/release-notes.md` — bullet under the 0.4 summary.
- **Validated:** `poetry run mkdocs build --strict` exits 0, no absolute/unrecognized-link INFO
  lines.
- **Cross-repo:** framework change + journal entry in `~/Code/uvicore/framework`
  (`journal/2026-07-01.md`), schematic stub updated with a commented `options` example.

## Document DB engine disposal at shutdown + idempotent init (framework 0.4.6)

- **Intent:** Document the framework 0.4.6 fix for `RuntimeError: Event loop is closed` on exit —
  engines now disposed via the Shutdown events, new `uvicore.db.disconnect()`, and re-init-safe
  `uvicore.db.init()` (reuse unchanged engines, dispose replaced ones, preserve MetaData).
- **Changed:** `docs/database/db-config.md` — new "Engine Lifecycle" section.
  `epologue/changelog/0.4.md` — new `## 0.4.6` section (above the released 0.4.5).
  `epologue/release-notes.md` — 0.4.6 bullet under the 0.4 summary.
- **Validated:** `mkdocs build --strict` — my three files produce no warnings; the build currently
  aborts on 3 pre-existing warnings from unrelated in-progress WIP in the working tree (untracked
  `docs/auth/` links to not-yet-written pages, and a `../../deeper/templating.md` link in the
  modified `deeper/ioc.md`), verified via stash that they exist without my changes.
- **Cross-repo:** framework change + journal entry in `~/Code/uvicore/framework`
  (`journal/2026-07-01.md`); 0.3 backport already released as 0.3.18.
