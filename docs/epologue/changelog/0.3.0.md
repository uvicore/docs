# v0.3.0

## :material-pound:  Summary




## OLD

The `driver` and `dialect` properties of the `config/database.py` connections were reversed.

We also replaced `driver` with `sync_driver` and `async_driver`

See [Upgrade 0.2 to 0.3](/epologue/upgrade-0.2-to-0.3/) for details.
